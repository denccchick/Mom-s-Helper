repos:
  - repo: local
    hooks:
      - id: run-pytests
        name: Run pytest (MomsHelperBackend)
        entry: python -m pytest -q MomsHelperBackend
        language: system
        pass_filenames: false
        stages: [push]
        always_run: true
